# 먹자 골목 관리 오류/예외 처리 기록

작성일: 2026-06-06  
테스트 기준: `docs/mukja-function-tree-ko.md`

이 문서는 기능 테스트 중 확인해야 할 예상 예외와 실제 수정/처리 내역을 기록합니다.

## 테스트 기준

기능 테스트는 기능 트리의 다음 영역을 기준으로 합니다.

- 공통: 로그인, 권한 제한, 사이드바, 언어 전환, 로그아웃, 캐시 초기화
- 홈: 최신 요청 조회, 부서 필터, 체크 저장, 요청별 메모
- 요청 내역: 주 단위 목록, 상세 화면, 카테고리 표시, 체크 수정
- 요청하기: 주문 모드, 수정 모드, 재료 CRUD, 카테고리 CRUD, 일괄 변경, 드래그
- 요청하기 메시지: 요청 저장 후 부서별 메시지 생성, 복사, 카카오톡 앱 열기
- 메뉴: 보기/수정 모드, 검색, 카테고리 필터, 가격 표시, 레시피 팝업, 메뉴 CRUD
- 레시피: 보기/수정 모드, 재료/순서 CRUD, 사진, 삭제, 드래그
- 관리자: 부서 CRUD, 비밀번호 CRUD, 데이터 백업
- 데이터/API: DB CRUD, CSV 변환, localStorage fallback

## 자동 테스트 결과

| 테스트 | 목적 | 최근 결과 |
| --- | --- | --- |
| `npm run test:user-flows` | 권한별 화면 렌더링, 접근 제한, 메뉴 가격 표시, 테스트 요청 생성/삭제 | 통과 |
| `npm run test:db-consistency` | 실제 DB 요청 C/R/U/D, 메모, 체크, 홈/요청 내역/상세 동기화 | 통과 |

주의: 두 테스트는 실제 DB에 임시 요청을 만들고 삭제합니다. 동시에 실행하면 서로의 임시 요청을 잠깐 볼 수 있으므로 최종 검증은 순차 실행합니다.

## 예상 예외와 처리 방식

| 영역 | 예외 상황 | 처리 방식 |
| --- | --- | --- |
| 로그인 | 잘못된 비밀번호 입력 | 오류 메시지 표시, 입력값 선택 |
| 권한 | 권한 없는 페이지 접근 | 사이드바 비활성화, 페이지에서는 권한 없음 메시지 표시 |
| 홈 | 저장된 요청 없음 | 빈 상태 메시지 표시 |
| 홈 | 타 부서 메모 수정 시도 | 읽기 전용으로 표시 |
| 홈 | DB 최신 데이터 지연 | 브라우저 포커스/탭 복귀 시 재조회 |
| 요청 내역 | 해당 주 데이터 없음 | 빈 상태 메시지 표시 |
| 요청 내역 | 상세 ID가 없거나 삭제됨 | 목록 화면으로 돌아가 확인 |
| 요청하기 | 권한 없는 사용자가 접근 | 권한 없음 메시지 표시 |
| 요청하기 | 선택 품목 없음 | 요청 저장을 막고 빈 요청 방지 |
| 요청하기 | 품목도 메모도 없는 상태에서 요청 클릭 | 요청 내역과 메모 모두 저장하지 않음 |
| 요청하기 | 메모만 입력 후 요청 클릭 | 요청 내역에 메모 요청으로 저장, 홈에는 메모 패널만 표시 |
| 요청하기 수정 | 같은 부서에 같은 품목명 추가/수정 | 팝업으로 중복을 알리고 저장 차단 |
| 요청하기 수정 | 재료 한글명 또는 영문명 누락 | 팝업으로 필수 입력을 알리고 저장 차단 |
| 요청하기 | 요청 메시지 생성 버튼 중복 | 별도 생성 버튼을 제거하고 `요청` 버튼 저장 후 자동 생성 |
| 요청하기 | 요청 메시지 품목이 길어져 확인 어려움 | 화면 카테고리/품목 순서대로 카테고리별 라인을 만들고 `필요합니다`는 마지막 줄로 분리 |
| 요청하기 | 카톡 버튼 위치 중복 | 메시지 헤더의 카톡 버튼을 제거하고 메시지 아래 노란색 버튼만 유지 |
| 요청하기 | 보기 모드에서도 긴 목록 이동이 불편함 | 부서/카테고리 접기/펼치기를 보기 모드에도 제공 |
| 요청하기 | 수정 폼 열린 상태에서 드래그 | 편집 폼이 열려 있는 동안 품목/카테고리 이동 전체 비활성화 |
| 요청하기 수정 | 카테고리 품목이 많아 이동이 불편함 | 카테고리 접기/펼치기, 제목 클릭 포커스, 접힌 상태 액션 최소화 |
| 요청하기 수정 | 부서 안 카테고리가 많아 전체 이동이 불편함 | 부서 제목 줄에서 해당 부서 카테고리를 한 번에 접고 펼침 |
| 요청하기 수정 | 카테고리 드롭 위치가 좁거나 불명확함 | 카테고리 기준선을 표시하고 품목 행 위에서도 카테고리 드롭이 전달되도록 범위 확대 |
| 요청하기 | 체크박스 1개 선택 후 수정 모드 | 불필요한 펼침 동작 방지 |
| 재료 DB | 기존 `section` 필드 데이터 | `category`로 변환, 구형 필드 fallback 유지 |
| 재료 DB | 카테고리 영문명 누락 | `categoryEn` 필드 추가 |
| 메뉴 DB | 메뉴 카테고리 영문명 누락 | `categoryEn` 필드 추가 |
| 메뉴 DB | DoorDash 불어/영어 혼합 | 불어 제거, 한글/영문 필드 분리 |
| 메뉴 DB | 한글 메뉴명에 영어 포함 | `nameKo` 한글명으로 정리 |
| 메뉴 수정 | 같은 메뉴명 추가/수정 | 팝업으로 중복을 알리고 저장 차단 |
| 메뉴 수정 | 메뉴 한글명 또는 영문명 누락 | 팝업으로 필수 입력을 알리고 저장 차단 |
| 메뉴 보기 | 가격 없음 | 가격 라벨 미표시 |
| 메뉴 보기/수정 | 카테고리 안 메뉴가 많아 이동이 불편함 | 메뉴 카테고리 접기/펼치기를 보기/수정 모드에 제공 |
| 메뉴 수정 | 새 메뉴 생성 | 빈 레시피 자동 생성 |
| 메뉴/레시피 | 수정 폼 열린 상태에서 드래그 | 해당 행 드래그 비활성화 |
| 레시피 | 사진 없는 조리 순서 | 사진 버튼 비활성화 |
| 관리자 | 마지막 관리자 삭제 | 삭제 방지 |
| 관리자 부서 | 사용 중인 부서 삭제 또는 비활성화 | 재료/요청 내역/메모/접근 계정 참조가 있으면 삭제/비활성화 차단 |
| 관리자 부서 | 부서명 변경 | 재료/요청 내역/메모/접근 계정의 부서 참조를 함께 이관 |
| 관리자 부서 | 사용 중 부서가 0개가 됨 | 저장 차단 |
| 관리자 부서 | 부서 목록에 영문명/사용 현황이 함께 표시됨 | 현재 언어의 부서명만 표시 |
| 데이터 백업 | 전체 백업 기능이 필요하지만 화면 버튼은 아직 원치 않음 | `Store.createCsvBackupBundle()` 함수만 구현하고 화면에는 미표시 |
| 사용자 DB | 비밀번호 기반 계정에서 회원 관리로 확장 필요 | `app_users` DB와 `access_accounts.user_id` 연결 추가, 회원 관리 화면은 미구현 |
| 일반 모드 | 요청 수량/단위 입력 확장 필요 | `order_items.quantity`, `order_items.unit` 및 CSV 컬럼으로 저장 구조 유지 |
| CSV Import | 빈 파일 또는 잘못된 CSV | Import 실패 메시지 표시 |
| CSV Import | 기존 데이터 교체 | 확인 창 후 실행 |
| DB/API | DB 연결 실패 | localStorage 보조 저장소 사용 |
| 캐시 | 이전 화면 표시 | `reset-cache.html`로 서비스워커/캐시 정리 |

## 이번 수정/처리 내역

### 1. 재료 스키마 정리

- `section` 중심이던 재료/요청 품목 분류를 `category`로 변경했습니다.
- 구형 데이터 호환을 위해 `section` fallback은 유지했습니다.
- 재료 카테고리 영어 필드 `categoryEn`을 추가했습니다.

### 2. 메뉴 데이터 정리

- DoorDash 메뉴 데이터를 메뉴 DB 스키마에 맞춰 Import했습니다.
- `Featured Items`, `Most Ordered`는 DoorDash 중복 노출 영역이라 실제 메뉴 DB에서는 제외했습니다.
- 불어/영어 혼합 설명은 영어만 남겼습니다.
- 한글 메뉴명 필드에 남아 있던 영어 항목 13개를 한글명으로 수정했습니다.
- 메뉴 보기 모드에서 메뉴명 옆 가격을 표시하도록 수정했습니다.

### 3. 드래그와 입력 충돌 방지

- 요청하기 수정 모드에서 입력 폼이 열린 행은 드래그되지 않도록 수정했습니다.
- 메뉴 수정 모드에서 입력 폼이 열린 행은 드래그되지 않도록 수정했습니다.
- 레시피 재료/조리 순서 수정 폼이 열린 행도 드래그되지 않도록 수정했습니다.

### 4. 요청하기 수정 모드 체크박스 동작

- 수정 모드에서 체크박스가 하나만 선택된 경우 불필요한 펼침 동작을 하지 않도록 수정했습니다.

### 5. 영어 UI 문구

- 영어 Request 플로팅 검색 창의 `Previous`를 `Pre`로 줄여 잘림을 방지했습니다.

### 6. 테스트 보강

- `scripts/user-flow-test.cjs`가 테스트용 요청을 직접 생성하고 삭제하도록 보강했습니다.
- 기존 요청 내역이 비어 있어도 권한별 홈/요청 내역 테스트가 안정적으로 통과하도록 수정했습니다.
- `scripts/db-consistency-test.cjs`는 실제 DB C/R/U/D와 화면 동기화를 검증합니다.
- 요청하기 테스트에서 `요청` 버튼 클릭 후 부서별 메시지 3개와 카카오톡 열기 링크 3개가 생성되는지 검증합니다.
- 요청하기 테스트에서 생성 메시지에 카테고리 라인이 포함되는지 검증합니다.
- 요청하기 테스트에서 보기 모드 부서/카테고리 접기와 펼치기를 검증합니다.
- 요청하기 테스트에서 재료 한글명/영문명 필수 입력을 검증합니다.
- 요청하기 테스트에서 같은 부서의 중복 품목명 저장이 팝업으로 막히는지 검증합니다.
- 메뉴 테스트에서 카테고리 접기/펼치기, 메뉴 한글명/영문명 필수 입력, 중복 메뉴명 저장 차단을 검증합니다.

### 7. 요청 메시지와 카카오톡 버튼 정리

- `요청 메시지 만들기` 별도 버튼을 제거했습니다.
- `요청` 버튼을 누르면 요청 저장 후 부서별 메시지가 자동 생성됩니다.
- 카카오톡 열기 버튼은 각 메시지 아래에 노란색 기본 버튼 스타일로 표시합니다.
- 메시지 헤더에 있던 중복 카카오톡 버튼은 제거했습니다.

### 8. 요청 메시지 문구 순서 정리

- 부서별 요청 메시지를 화면에 저장된 카테고리 순서와 품목 순서 기준으로 생성하도록 수정했습니다.
- 품목은 카테고리별 라인으로 묶어 표시합니다.
- `필요합니다` 문구는 품목 라인 뒤 마지막 줄에 표시합니다.

### 9. 중복 이름 저장 방지

- 요청하기 수정 모드에서 같은 부서에 이미 같은 품목명이 있으면 저장하지 않고 팝업을 표시합니다.
- 메뉴 수정 모드에서 이미 같은 한글 메뉴명이 있으면 저장하지 않고 팝업을 표시합니다.
- 기존 항목을 수정할 때 자기 자신은 중복 검사에서 제외합니다.

### 10. 요청하기 편집 폼 드래그 잠금 보강

- 요청하기 수정 모드에서 품목/카테고리 편집 폼이 열려 있는 동안 전체 품목/카테고리 이동을 비활성화했습니다.
- 복사/붙여넣기를 위해 입력값을 드래그할 때 주변 리스트가 이동하지 않도록 드래그 시작 이벤트도 차단했습니다.
- 자동 테스트에서 편집 폼이 열린 동안 `draggable=true` 행이 남지 않는지 확인합니다.

### 11. 요청하기 카테고리 접기/펼치기

- 요청하기 수정 모드의 카테고리 제목 줄에 접기/펼치기 버튼을 추가했습니다.
- 접힌 카테고리는 품목 목록과 추가/수정 버튼을 숨기고, 펼침 버튼과 이동 핸들만 표시합니다.
- 카테고리명을 누르면 해당 카테고리로 포커스가 이동해 긴 목록에서 위치를 확인하기 쉽습니다.

### 12. 요청하기 부서별 접기/펼치기와 이동 위치 표시

- 요청하기의 부서 제목 줄에 전체 접기/펼치기 버튼을 추가했습니다.
- 요청하기 보기 모드에서도 부서/카테고리 접기와 펼치기가 가능하도록 수정했습니다.
- 카테고리 추가는 텍스트 버튼 대신 원형 `+` 아이콘으로 정리했습니다.
- 카테고리 사이에 회색 기준선을 표시하고, 드롭 후보 위치는 노란색 선으로 표시합니다.
- 카테고리 드롭을 품목 행 위에서도 처리할 수 있도록 이벤트 전달 범위를 넓혔습니다.
- 자동 테스트에서 부서별 전체 접기/펼치기와 카테고리 추가 아이콘을 검증합니다.

### 13. 접기/펼치기 아이콘과 메뉴 카테고리 UX 통일

- 접기/펼치기 아이콘은 같은 삼각형 SVG를 회전해 사용하도록 통일했습니다.
- 원형 버튼 안에서 밑줄과 삼각형 밑변 너비를 맞추고 선 두께를 키워 가시성을 높였습니다.
- 주변 액션 아이콘과 시각 무게가 맞도록 내부 도형 폭을 키웠습니다.
- 메뉴 페이지의 카테고리 제목 줄에도 보기/수정 모드 공통 접기/펼치기 버튼을 추가했습니다.
- 메뉴 카테고리를 접으면 메뉴 목록과 추가 버튼을 숨겨 긴 목록에서 이동하기 쉽게 했습니다.
- 자동 테스트에서 메뉴 카테고리 접기/펼치기를 검증합니다.

### 14. 빈 요청 저장 차단과 메모 전용 저장

- 요청하기에서 품목과 메모가 모두 비어 있으면 요청 내역 DB에 저장하지 않습니다.
- 메모만 입력한 경우에도 요청 내역 row로 저장하고 품목 목록 없이 메모만 표시합니다.
- 홈 화면은 메모만 있는 요청 내역도 최신 요청으로 표시합니다.
- 자동 테스트에서 빈 요청 저장 차단과 메모만 저장 시 요청 내역에 메모 요청이 남는지 검증합니다.

### 15. 재료/메뉴 필수 이름 검증

- 요청하기 수정 모드의 재료 품목은 한글명과 영문명이 모두 있어야 저장됩니다.
- 메뉴 수정 모드의 메뉴는 한글명과 영문명이 모두 있어야 저장됩니다.
- 누락 시 팝업으로 필수 입력 항목을 알리고 DB 저장을 차단합니다.
- 자동 테스트에서 재료/메뉴 한글명과 영문명 누락 저장 차단을 검증합니다.

### 16. 부서 DB 관리

- 부서 목록을 `app_settings.departments` 설정 데이터로 저장하도록 추가했습니다.
- 관리자 페이지에서 부서 한글명, 영문명, 사용 여부를 관리할 수 있습니다.
- 입장 비밀번호의 부서 선택지는 하드코딩된 목록이 아니라 부서 DB의 사용 중 부서 기준으로 표시합니다.
- 요청하기, 홈, 요청 내역, 부서별 메모는 부서 DB 순서와 부서명을 기준으로 표시합니다.
- 부서명 변경 시 재료, 요청 내역, 요청 메모, 구버전/보조 메모, 접근 계정의 부서 참조를 함께 이관합니다.
- 사용 중인 부서는 삭제하거나 비활성화할 수 없고, 사용 중 부서가 하나도 없도록 저장할 수 없습니다.
- 자동 테스트에서 관리자 부서 관리 영역 표시와 `app_settings.departments` 설정 저장을 검증합니다.

### 17. 일반 모드와 회원 관리 확장 기반

- 일반 모드에서 사용할 요청 수량/단위는 이미 요청 품목 DB와 CSV에 저장되는 구조임을 확인했습니다.
- 추후 회원 관리를 위해 `app_users` 사용자 DB를 추가했습니다.
- 기존 비밀번호 관리는 계속 `access_accounts`에서 동작하지만, 저장 시 대응되는 `app_users` row와 `access_accounts.user_id` 연결을 유지합니다.
- 회원 관리 화면과 로그인 방식 변경은 아직 구현하지 않았습니다.
- 전체 CSV 백업 묶음 생성 함수 `Store.createCsvBackupBundle()`를 추가했습니다.
- 해당 백업 함수는 테스트에서 검증하지만, 관리자 화면에는 아직 버튼으로 표시하지 않습니다.
- 관리자 페이지 순서를 부서 관리, 비밀번호 관리, 데이터 백업 순서로 정리했습니다.
- 부서 목록은 한글 화면에서는 한글 부서명만, 영문 화면에서는 영문 부서명만 표시합니다.

### 18. 부서 배지와 매장/먹자 부서 정리

- 부서 관리 목록의 표시 순서를 번호, 부서명, 아이콘 순서로 변경했습니다.
- 부서 배지는 한글 첫 글자가 아니라 기존 영문 코드인 C/V/G/R/A를 사용하고, 먹자는 M으로 표시합니다.
- 카페테리아 배지는 갈색, 야채 배지는 녹색, 매장 배지는 주황색, 먹자 배지는 먹자 로고 계열 배경색으로 표시합니다.
- 기존 그로서리 데이터는 화면과 신규 저장 로직에서 매장으로 정규화합니다.
- 부서 DB 기본값에 먹자를 추가하고 매장 카테고리와 별도 기타 카테고리를 유지합니다.

### 19. 홈 최신 요청 우선 표시와 관리자 섹션 접기

- 홈 화면의 최신 표시 기준을 `date/time` 문자열 단독 정렬에서 DB `updatedAt/createdAt` 우선 정렬로 변경했습니다.
- 요청 내역이 하나라도 있으면 홈은 메모 전용 화면보다 최신 요청 내역을 우선 표시합니다.
- 요청 품목이 없는 메모 전용 화면은 표시할 요청 내역이 없을 때만 사용합니다.
- 관리자 페이지의 부서 관리, 비밀번호 관리, 데이터 백업 섹션에 접기/펼치기 버튼을 추가했습니다.
- 관리자 섹션 간 위아래 간격을 넓혀 각 관리 영역이 더 분리되어 보이도록 조정했습니다.
- 자동 테스트에서 홈 최신 요청 품목 표시와 관리자 섹션 접기/펼치기를 검증합니다.

### 20. 요청하기 취소와 메모 요청 저장 기준

- 요청하기에 취소 버튼을 추가해 저장 전 변경 내용을 버리고 마지막 저장 상태를 다시 불러올 수 있게 했습니다.
- 요청하기의 마지막 요청 선택 기준을 홈과 동일하게 `updatedAt/createdAt` 우선으로 맞췄습니다.
- 요청하기에서 메모만 입력해도 요청 내역 row로 저장하고 홈/요청 내역에 표시되도록 변경했습니다.
- 요청하기에서 마지막 요청을 수정해 저장하면 같은 요청의 마지막 상태로 업데이트합니다.

### 21. V1.0 마감 문서와 버전 표시

- 사이드바 하단에 `Version 1.0` 표시를 추가했습니다.
- 접힌 사이드바에서는 공간을 줄이기 위해 `V1.0`으로 표시합니다.
- 정적 자산 버전을 `v193`으로 올려 V1.0 표시와 문서 기준이 배포 캐시에 반영되도록 했습니다.
- README, Manual, 기능 트리를 V1.0 운영 기준과 다음 개발 브랜치 기준으로 정리했습니다.
- 요청하기 마지막 요청 업데이트, 빈 요청 차단, 메모만 요청 저장, 취소 복구 기준을 문서에 다시 맞췄습니다.

### 22. 요청 날짜 UTC 밀림 수정

- 토론토 밤 시간에 요청하면 `Store.today()`가 UTC 기준 날짜를 사용해 다음 날로 저장되는 문제가 있었습니다.
- 요청 내역 화면은 화요일부터 일요일까지 현지 날짜 기준으로 묶기 때문에, 실제 저장은 되었지만 현재 주 목록에서 빠져 보일 수 있었습니다.
- 요청 날짜 생성과 요청 내역 날짜 표시를 브라우저 현지 날짜 기준으로 변경했습니다.
- 서버 fallback 날짜도 `America/Toronto` 기준으로 보강했습니다.
- 정적 자산 버전을 `v194`로 올려 수정된 날짜 로직이 배포 캐시에 반영되도록 했습니다.
- 수정 전 요청 내역 백업: `/Users/itsfree/Downloads/mukja-db-backups/history-before-date-fix-2026-06-07T21-43.json`

### 23. 카카오톡 열기 버튼에 메시지 복사 통합

- 부서별 메시지 카드의 별도 `문구 복사` 버튼을 제거했습니다.
- 메시지 아래 버튼명을 `문자 복사 / 카톡 열기`로 변경했습니다.
- 해당 버튼을 누르면 부서별 메시지를 먼저 클립보드에 복사하고, 복사 성공 후 카카오톡 앱을 엽니다.
- 자동으로 특정 카카오톡 채팅방에 전송하지는 않고, 사용자가 열린 카카오톡에서 붙여넣어 보냅니다.
- 자동 테스트에서 부서별 통합 버튼 3개 표시와 별도 복사 버튼 제거를 검증합니다.

### 24. 요청 총 품목 수 표시

- `요청` 버튼으로 저장이 성공하면 요청 버튼과 부서별 요청 메시지 섹션 사이에 `총 N개의 품목을 요청합니다.` 문구를 표시합니다.
- 표시 숫자는 저장한 요청 품목 배열의 총 개수 기준입니다.
- 품목이 없는 메모-only 요청, 빈 요청 차단, 초기화, 취소 시에는 총 품목 수 문구를 숨깁니다.
- 자동 테스트에서 빈 요청/메모-only 요청에서는 숨김, 품목 요청에서는 총 3개 문구 표시를 검증합니다.

### 25. 요청하기 `맨위` 이동 위치 조정

- 카테고리 이동 패널의 `맨위` 버튼이 첫 카테고리로 이동해 주문/수정 모드 버튼을 바로 누르기 어려웠습니다.
- `맨위` 버튼을 누르면 요청하기 제목과 모드 버튼 영역으로 이동하도록 변경했습니다.
- `맨아래`, 이전, 다음, 이동 버튼은 기존 카테고리 이동 동작을 유지합니다.
- 자동 테스트에서 `맨위` 버튼이 요청하기 제목 영역을 포커스하는지 검증합니다.

### 26. 홈 이동 컨트롤과 접기/펼치기

- 관리자와 레스토랑 권한의 홈 화면에서 마지막 요청 제목 아래에 요청하기와 같은 이동 컨트롤을 추가했습니다.
- 홈 이동 컨트롤은 부서와 카테고리를 선택한 뒤 현재/이전/다음/맨위/맨아래 이동을 지원합니다.
- 홈 화면의 부서 제목 줄에서 해당 부서의 모든 카테고리를 한 번에 접고 펼칠 수 있습니다.
- 홈 화면의 카테고리 제목 줄에서도 품목 목록을 접고 펼칠 수 있습니다.
- 일반 부서 권한에는 홈 이동 컨트롤과 접기 버튼을 노출하지 않습니다.
- 자동 테스트에서 관리자/레스토랑 홈 컨트롤 표시, 부서/카테고리 접기, 이동 포커스, 일반 부서 권한 숨김을 검증합니다.

### 27. 부서별 `기타` 카테고리 보호

- 모든 부서의 요청 카테고리에 `기타`가 항상 포함되도록 보정했습니다.
- 카테고리를 삭제하면 품목은 삭제하지 않고 같은 부서의 `기타`로 이동하도록 변경했습니다.
- `기타` 카테고리는 삭제하거나 이름을 바꿀 수 없도록 차단했습니다.
- 잘못된 카테고리 값이 들어온 경우 홈/요청 내역/요청하기 화면에서 `기타`로 묶이도록 fallback을 맞췄습니다.
- 자동 테스트에서 부서별 `기타` 존재, 삭제 버튼 숨김, 이름 변경 차단을 검증합니다.

### 28. 부서별 카테고리/품목 중복 저장 차단

- 카테고리 생성/수정 시 같은 부서 안에 같은 카테고리명이 있으면 팝업을 띄우고 저장하지 않습니다.
- 품목 생성/수정 시 같은 부서 안에 같은 한글명 또는 영문명이 있으면 팝업을 띄우고 저장하지 않습니다.
- 공백 차이와 영문 대소문자 차이는 같은 이름으로 보고 중복을 차단합니다.
- 자동 테스트에서 카테고리 중복, 품목 한글명 중복, 품목 영문명 중복 차단을 검증합니다.

## 남은 수동 확인 항목

- 모바일 폭에서 사이드바와 버튼 텍스트 겹침 확인
- Admin CSV Import 버튼으로 실제 CSV 파일 선택 후 확인
- 메뉴/레시피 팝업의 실제 이미지 렌더링 확인
- DB 초기화 후 운영 CSV Import 전체 흐름 확인

## 운영 전 권장 대응

1. `/Users/itsfree/Downloads/mukja-db-backups`에 최신 DB 백업을 남깁니다.
2. 기능 트리 기준으로 화면을 캡처하고 `Manual.md`에 이미지 링크를 반영합니다.
3. 자동 테스트를 순차 실행합니다.
4. CSV Import 전에는 반드시 Export 파일을 먼저 저장합니다.
5. 오류가 생기면 이 문서에 재현 조건, 수정 파일, 테스트 결과를 추가합니다.
